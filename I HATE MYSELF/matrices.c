#include <stdio.h>

int main() {
    //taking input of a 1D matrix
    /*
    int n;
    printf("Enter the size of the 1D matrix: ");
    scanf("%d", &n);
    int arr[n];
    printf("Enter the elements of the 1D matrix:\n");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }
    printf("The elements of the 1D matrix are:\n");
    for (int i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    return 0;
    */

    
}