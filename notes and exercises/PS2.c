#include <stdio.h>
int main() {
    //Q3] write a program to check whether a given number is divisible by 97 or not.
    /*
    int n;
    printf("Enter a positive integer: ");
    scanf("%d", &n);
    if(n%97==0)
        printf("%d is divisible by 97\n", n);
    else
        printf("%d is not divisible by 97\n", n);
    return 0;
    */
    
    
}